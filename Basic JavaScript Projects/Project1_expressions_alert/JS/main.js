var hello = "Hello";
var js = "JavaScript";

// Concatenate the String variables and insert a space between them
var msg = hello + " " + js;
window.alert(msg);

var greenMsg = msg.fontcolor("green"); // The message in green
document.write(greenMsg); // Write the green message to the screen
